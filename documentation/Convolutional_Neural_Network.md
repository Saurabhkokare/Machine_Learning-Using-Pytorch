# CNN Image Classification with PyTorch

This project trains a **Convolutional Neural Network (CNN)** using PyTorch to classify images.

---

## Requirements

- Python 3.8+
- PyTorch
- OpenCV
- NumPy

Install dependencies:

```bash
pip install torch torchvision numpy opencv-python
