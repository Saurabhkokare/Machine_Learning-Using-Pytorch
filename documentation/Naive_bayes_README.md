# Gaussian Naive Bayes from Scratch (PyTorch)

This project implements a **Gaussian Naive Bayes classifier from scratch using PyTorch tensors** (without using `sklearn`).  
It demonstrates the **core theory, training, prediction, evaluation, and model saving** process for supervised learning.

---

## 📌 Features

- Gaussian Naive Bayes implemented from scratch
- Uses PyTorch tensors for numerical computation
- Binary classification example
- Train–test split
- Accuracy evaluation
- Model saving and loading
- Simple and interview-friendly code structure

---